# Icons

Always respect the project's configured icon library.

Rules:

- use the configured library, not assumed `lucide-react`
- in buttons, use `data-icon="inline-start"` or `data-icon="inline-end"`
- avoid manual sizing classes inside shadcn components unless the user asked for custom sizes
- pass icon components as values, not string names
